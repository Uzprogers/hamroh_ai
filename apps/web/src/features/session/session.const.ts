import type { TranslationKey } from "../../i18n/dictionary";

export const EXERCISE_PASS_PERCENT = 50;

export const BUILD_STAGES: TranslationKey[] = [
  "build.stage.topic",
  "build.stage.tasks",
  "build.stage.answers",
];

export const BUILD_STAGE_MS = 2600;
export const BUILD_ROWS = ["72%", "88%", "64%", "80%"];
